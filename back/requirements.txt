fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
PyJWT>=2.8,<3.0
pydantic>=2.7,<3.0
httpx2>=2.0,<3.0

# Detecção de ações via esqueleto (src/control) — só CPU, sem envio de dados à nuvem.
# mediapipe já traz o opencv-contrib-python (cv2) como dependência.
--extra-index-url https://download.pytorch.org/whl/cpu
numpy>=1.26,<3.0
torch>=2.2,<3.0
mediapipe>=1.0,<2.0
